connections:
  pg-test:
    type: PostgreSQL
    host: localhost
    port: "5432"
    database: testdb
    user: testuser
    password: testpass

  gcs-test:
    type: CloudStorage
    provider: gcs
    bucket: test-bucket
